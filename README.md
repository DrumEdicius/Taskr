vibeCoded task app, simple features.

Features:
/nAdd tasks with priority levels (Low, Medium, High)
/nMark tasks as complete with a satisfying checkmark
/nDelete tasks
/nFilter by All / Pending / Done / High priority
/nLive progress bar and stats counter
/nTasks save automatically (localStorage) so they persist on refresh
